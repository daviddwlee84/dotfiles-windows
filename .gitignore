# mkdocs build output
site/

# python / uv
.venv/
__pycache__/
*.pyc
uv.lock

# os
.DS_Store
Thumbs.db

# dockur/windows VM disk
docker/windows/storage/

# agent/session artifacts (not part of the dotfiles)
# .claude/
# ephemeral EnterWorktree checkouts; never commit
.claude/worktrees/
# .specstory/
# SpecStory machine-local identity and generated statistics are not review history.
/.specstory/.project.json
/.specstory/statistics.json

# local, untracked user overrides that may get copied here by accident
*.local.ps1

# Pester -CI test report artifact (justfile `test`)
testResults.xml
