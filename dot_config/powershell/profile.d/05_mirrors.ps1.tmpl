# 05_mirrors.ps1 — company-approved or China package registries.
# The shared body is also embedded in the apply-time package installer. Keep the
# historical target name so an upgrade does not leave a stale unmanaged fragment.
{{ template "package-sources.ps1" . }}
